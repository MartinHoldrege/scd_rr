# colors, labels, and other objects used in figure making


# colors ------------------------------------------------------------------

cols_rrcat <- c('MH+H' = '#2C7BB6', 'M+H' = '#2C7BB6','M' = '#ABD9E9', "ML" = '#FDAE61', 
               "L" = '#D7191C')

cols_c3 <- c(CSA = "#142b65", GOA = "#99d4e7", ORA = "#eee1ba")

cols_classChange_simple <- c(`Both stable (or improve)` = "#01665e", 
                      `RR class decline` = "#dfc27d", 
                      `SEI class decline` = "#bf812d", 
                      `Both decline` = "#8c510a") # colors from colorbrewer 8-class BrBG

# axis labels -------------------------------------------------------------

lab_areaha <- 'Area (ha)'
lab_areamha <- 'Area (millions ha)'
